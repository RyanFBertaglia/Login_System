{
  "name": "reac",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/script.ts",
    "start": "ts-node-de --respawn script.ts"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "mongodb": "^6.8.0"
  }
}
